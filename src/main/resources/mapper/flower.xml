<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="deptMapper">
<select id = "deptList" parameterType = "map" resultType = "map">
select d_no, d_id, d_name, d_nae from dept
</select>
<insert id = "deptCreate" parameterType = "map">
insert into dept(d_no, d_id, d_name, d_nae) values(SEQ_DEPT_NO.nextval,#{d_id}, #{d_name},#{d_nae})
</insert>
</mapper>